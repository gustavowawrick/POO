import { area, circunferencia as circ } from "./definicaoES603A.mjs";

console.log(`Área do círculo de raio 4: ${area(4)}`);
console.log(`Circunferência do círculo de raio 4: ${circ(4)}`);

import * as circulo from "./definicaoES603A.mjs";

console.log(`Área do círculo de raio 2: ${circulo.area(2)}`);